<!DOCTYPE html>
<html lang="fr">
	<head>
		<meta charset="UTF-8">
		<meta http-equiv="X-UA-Compatible" content="IE=edge">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link rel="stylesheet" href="{{asset('css/style.css')}}">
		<title>Document</title>
	</head>
	<body>

	{% for grproduct in gproducts.getAll %}
			<h3>{{ grproduct.name }}</h3>
		{% endfor %}

		{% for grbrand in gbrands.getAll %}
			<h3>{{ grbrand.name }}</h3>
		{% endfor %}

		{% for grcategories in gcategories.getAll %}
			<h3>{{ grcategories.name }}</h3>
		{% endfor %}

		{% block main %} 
<h1> {{ front }} </h1>
		
		{% endblock %}

	</body>
</html>