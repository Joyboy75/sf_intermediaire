{% extends "front/base.html.twig" %}

{% block main %}

	<h2>{{ product.name }}</h2>
	<h3>{{ product.price }}</h3>
    <h4>{{product.category.name}}</h4>
    <h4>{{product.brand.name}}</h4>

{% endblock %}
